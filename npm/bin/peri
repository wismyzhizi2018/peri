#!/bin/sh
dir="$(dirname "$(readlink -f "$0" 2>/dev/null || realpath "$0" 2>/dev/null || echo "$0")")"
if [ -f "${dir}/peri-bin" ]; then
  exec "${dir}/peri-bin" "$@"
else
  exec "${dir}/peri.exe" "$@"
fi
